import { Logger, getObjectId, objectsEqual, dateDaySearch } from '../../utilities/generic';
import Service from '../../utilities/service';

class TripService extends Service {

  /*
  * Access a list of objects
  * @param {Object} query
  * @return {Array} objects
  */
  // async index(query) {
    
  // }

  /*
  * Show a particular object using the id
  * @param {ObjectId} id
  * @return {Object} object
  */
  // async show(id) {
    
  // }
}

module.exports = TripService;